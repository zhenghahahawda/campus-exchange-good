# 校园交换系统数据库设计报告
## Campus Exchange System Database Design

---

## 一、系统概述

校园交换系统是一个面向校园用户的物品交换平台，支持用户发布闲置物品、浏览商品、发起交换请求、在线沟通、订单管理、违规举报等功能。

### 设计原则

1. **数据完整性**：使用外键约束保证数据一致性
2. **性能优化**：合理设置索引提升查询效率
3. **安全性**：密码加密存储、会话管理、操作日志
4. **可扩展性**：预留扩展字段、支持多种业务场景

---

## 二、数据表详细设计

### 2.1 用户管理模块

#### 2.1.1 users - 用户表

**表说明**：存储用户账号信息、认证数据、信用评分

**字段列表**：

| 字段名 | 数据类型 | 是否为空 | 默认值 | 注释 |
|--------|---------|---------|--------|------|
| user_id | int UNSIGNED | NO | - | 用户唯一ID，8位数字（10000000-99999999） |
| username | varchar(50) | NO | - | 用户名，用于登录，3-50字符 |
| password_hash | varchar(255) | NO | - | 密码哈希值（bcrypt加密） |
| email | varchar(100) | NO | - | 邮箱地址 |
| phone | varchar(20) | YES | NULL | 手机号码 |
| user_type | tinyint UNSIGNED | NO | 2 | 用户类型：1=管理员，2=普通用户 |
| status | tinyint UNSIGNED | NO | 1 | 账户状态：0=禁用，1=正常，2=锁定 |
| credit_score | tinyint UNSIGNED | NO | 80 | 用户信誉值（1-100，默认80） |
| is_banned | tinyint UNSIGNED | NO | 0 | 是否被封禁：0=正常，1=已封禁 |
| banned_until | timestamp | YES | NULL | 封禁截止时间 |
| ban_reason | varchar(500) | YES | NULL | 封禁原因 |
| real_name | varchar(50) | YES | NULL | 真实姓名 |
| avatar_url | varchar(500) | YES | NULL | 头像URL地址（OSS存储） |
| address | varchar(255) | YES | NULL | 用户地址（详细地址信息） |
| school | varchar(100) | YES | NULL | 学校名称（用于校园交易场景） |
| gender | tinyint UNSIGNED | YES | NULL | 性别：0=未知，1=男，2=女 |
| birthday | date | YES | NULL | 出生日期 |
| created_at | timestamp | NO | CURRENT_TIMESTAMP | 账户创建时间 |
| updated_at | timestamp | NO | CURRENT_TIMESTAMP | 最后更新时间 |
| last_login_at | timestamp | YES | NULL | 最后登录时间 |
| login_attempts | tinyint UNSIGNED | NO | 0 | 连续登录失败次数 |
| locked_until | timestamp | YES | NULL | 账户锁定截止时间 |
| deleted_at | timestamp | YES | NULL | 软删除时间 |

**索引**：
- PRIMARY KEY: user_id
- UNIQUE: username, email, phone
- INDEX: user_type, status, credit_score, is_banned, school

---


#### 2.1.2 user_id_sequence - 用户ID序列表

**表说明**：生成10000000-99999999范围的顺序用户ID

**字段列表**：

| 字段名 | 数据类型 | 是否为空 | 默认值 | 注释 |
|--------|---------|---------|--------|------|
| id | int UNSIGNED | NO | - | 当前序列值 |
| stub | char(1) | NO | '' | 占位字符 |

**索引**：
- PRIMARY KEY: id
- UNIQUE: stub

---

#### 2.1.3 user_sessions - 用户会话表

**表说明**：管理用户登录会话、token认证

**字段列表**：

| 字段名 | 数据类型 | 是否为空 | 默认值 | 注释 |
|--------|---------|---------|--------|------|
| session_id | varchar(64) | NO | - | 会话唯一ID |
| user_id | int UNSIGNED | NO | - | 用户ID |
| token | varchar(2000) | NO | - | 令牌 |
| refresh_token | varchar(2000) | YES | NULL | 刷新令牌 |
| created_at | timestamp | NO | CURRENT_TIMESTAMP | 会话创建时间 |
| expires_at | timestamp | NO | - | 会话过期时间 |
| last_activity_at | timestamp | NO | CURRENT_TIMESTAMP | 最后活动时间 |
| ip_address | varchar(45) | NO | - | 登录IP地址 |
| user_agent | text | YES | NULL | 用户代理字符串 |
| device_id | varchar(100) | YES | NULL | 设备唯一标识 |
| is_active | tinyint UNSIGNED | NO | 1 | 是否活跃：0=已失效，1=活跃中 |

**索引**：
- PRIMARY KEY: session_id
- INDEX: user_id, expires_at, is_active
- COMPOSITE INDEX: (user_id, is_active)

**外键**：
- user_id → users(user_id) ON DELETE CASCADE

---

#### 2.1.4 user_login_logs - 登录日志表

**表说明**：记录用户登录历史、审计追踪

**字段列表**：

| 字段名 | 数据类型 | 是否为空 | 默认值 | 注释 |
|--------|---------|---------|--------|------|
| log_id | bigint UNSIGNED | NO | - | 日志唯一ID |
| user_id | int UNSIGNED | NO | - | 用户ID |
| login_time | timestamp | NO | CURRENT_TIMESTAMP | 登录时间 |
| login_ip | varchar(45) | NO | - | 登录IP地址（支持IPv4和IPv6） |
| login_status | tinyint UNSIGNED | NO | - | 登录状态：0=失败，1=成功 |
| fail_reason | varchar(100) | YES | NULL | 登录失败原因 |
| user_agent | text | YES | NULL | 浏览器User-Agent字符串 |
| device_type | varchar(20) | YES | NULL | 设备类型：PC/Mobile/Tablet |
| browser | varchar(50) | YES | NULL | 浏览器名称和版本 |
| os | varchar(50) | YES | NULL | 操作系统名称和版本 |

**索引**：
- PRIMARY KEY: log_id
- INDEX: user_id, login_time, login_status
- COMPOSITE INDEX: (user_id, login_time)

**外键**：
- user_id → users(user_id) ON DELETE CASCADE

---


### 2.2 商品管理模块

#### 2.2.1 goods - 商品表

**表说明**：存储用户发布的交换物品信息

**字段列表**：

| 字段名 | 数据类型 | 是否为空 | 默认值 | 注释 |
|--------|---------|---------|--------|------|
| good_id | int UNSIGNED | NO | - | 商品唯一ID |
| good_name | varchar(100) | NO | - | 商品名称 |
| description | text | YES | NULL | 商品详细描述 |
| exchange_for | varchar(255) | YES | NULL | 期望交换的物品描述 |
| category_id | tinyint UNSIGNED | NO | - | 商品分类ID |
| condition_level | tinyint UNSIGNED | NO | - | 新旧程度：1=全新，2=几乎全新，3=良好，4=一般 |
| status | tinyint UNSIGNED | NO | 0 | 商品状态：0=待审核，1=已通过，2=已拒绝 |
| shelf_status | tinyint UNSIGNED | NO | 0 | 0=已下架，1=已上架，2=已售出 |
| user_id | int UNSIGNED | NO | - | 卖家用户ID |
| images | json | YES | NULL | 商品图片URL数组，最多5张 |
| exchange_code | varchar(50) | YES | NULL | 交换凭证码 |
| view_count | int UNSIGNED | NO | 0 | 浏览次数 |
| like_count | int UNSIGNED | NO | 0 | 点赞数 |
| favorite_count | int UNSIGNED | NO | 0 | 收藏数 |
| reject_reason | varchar(500) | YES | NULL | 驳回原因 |
| auditor_id | int UNSIGNED | YES | NULL | 审核员ID |
| audited_at | timestamp | YES | NULL | 审核时间 |
| created_at | timestamp | NO | CURRENT_TIMESTAMP | 创建时间 |
| updated_at | timestamp | NO | CURRENT_TIMESTAMP | 更新时间 |
| sold_at | timestamp | YES | NULL | 售出时间 |
| deleted_at | timestamp | YES | NULL | 软删除时间 |

**索引**：
- PRIMARY KEY: good_id
- INDEX: user_id, category_id, status, shelf_status, created_at
- COMPOSITE INDEX: (status, shelf_status), (category_id, status)

**外键**：
- user_id → users(user_id) ON DELETE CASCADE
- category_id → goods_categories(category_id)
- auditor_id → users(user_id)

---

#### 2.2.2 goods_categories - 商品分类表

**表说明**：定义商品分类体系

**字段列表**：

| 字段名 | 数据类型 | 是否为空 | 默认值 | 注释 |
|--------|---------|---------|--------|------|
| category_id | tinyint UNSIGNED | NO | - | 分类ID |
| category_name | varchar(50) | NO | - | 分类名称 |
| category_code | varchar(20) | NO | - | 分类代码 |
| sort_order | tinyint UNSIGNED | NO | 0 | 排序顺序 |
| is_active | tinyint UNSIGNED | NO | 1 | 是否启用：0=禁用，1=启用 |
| created_at | timestamp | NO | CURRENT_TIMESTAMP | 创建时间 |

**索引**：
- PRIMARY KEY: category_id
- UNIQUE: category_code
- INDEX: is_active, sort_order

---


### 2.3 交易订单模块

#### 2.3.1 orders - 订单表

**表说明**：管理用户间的物品交换流程

**字段列表**：

| 字段名 | 数据类型 | 是否为空 | 默认值 | 注释 |
|--------|---------|---------|--------|------|
| order_id | int UNSIGNED | NO | - | 订单唯一ID |
| order_number | varchar(50) | NO | - | 订单号，格式：ORD-{时间戳}-{随机码} |
| conversation_id | varchar(50) | YES | NULL | 关联的会话ID |
| status | varchar(20) | NO | 'pending' | 订单状态：pending/completed/to_process/processing/cancelled |
| initiator_id | int UNSIGNED | NO | - | 发起方用户ID |
| initiator_good_id | int UNSIGNED | NO | - | 发起方提供的商品ID |
| receiver_id | int UNSIGNED | NO | - | 接收方用户ID |
| receiver_good_id | int UNSIGNED | NO | - | 接收方提供的商品ID |
| exchange_code | varchar(50) | YES | NULL | 交换凭证码 |
| initiator_confirmed | tinyint UNSIGNED | NO | 0 | 发起方是否已提交凭证码 |
| receiver_confirmed | tinyint UNSIGNED | NO | 0 | 接收方是否已提交凭证码 |
| remark | varchar(500) | YES | NULL | 订单备注（管理员可编辑） |
| cancel_reason | varchar(500) | YES | NULL | 取消原因 |
| admin_note | varchar(500) | YES | NULL | 管理员处理备注 |
| created_at | timestamp | NO | CURRENT_TIMESTAMP | 订单创建时间 |
| updated_at | timestamp | NO | CURRENT_TIMESTAMP | 订单更新时间 |
| completed_at | timestamp | YES | NULL | 订单完成时间 |
| cancelled_at | timestamp | YES | NULL | 订单取消时间 |
| deleted_at | timestamp | YES | NULL | 软删除时间 |
| initiator_review | varchar(500) | YES | NULL | 发起方评价内容 |
| initiator_review_images | varchar(1000) | YES | NULL | 发起方评价图片，逗号分隔，最多3张 |
| receiver_review | varchar(500) | YES | NULL | 接收方评价内容 |
| receiver_review_images | varchar(1000) | YES | NULL | 接收方评价图片，逗号分隔，最多3张 |
| initiator_reviewed_at | timestamp | YES | NULL | 发起方评价时间 |
| receiver_reviewed_at | timestamp | YES | NULL | 接收方评价时间 |

**索引**：
- PRIMARY KEY: order_id
- UNIQUE: order_number
- INDEX: initiator_id, receiver_id, status, created_at, exchange_code

**外键**：
- initiator_id → users(user_id) ON DELETE CASCADE
- receiver_id → users(user_id) ON DELETE CASCADE
- initiator_good_id → goods(good_id) ON DELETE CASCADE
- receiver_good_id → goods(good_id) ON DELETE CASCADE

---


### 2.4 即时通讯模块

#### 2.4.1 conversations - 会话表

**表说明**：管理私聊和群聊会话

**字段列表**：

| 字段名 | 数据类型 | 是否为空 | 默认值 | 注释 |
|--------|---------|---------|--------|------|
| id | varchar(50) | NO | - | 会话唯一ID |
| type | varchar(20) | NO | 'private' | 会话类型：private=私聊，group=群聊 |
| goods_id | int UNSIGNED | YES | NULL | 关联的商品ID |
| name | varchar(100) | YES | NULL | 会话名称 |
| avatar | varchar(500) | YES | NULL | 会话头像URL |
| last_message_id | varchar(50) | YES | NULL | 最后一条消息ID |
| last_message_time | timestamp | YES | NULL | 最后消息时间 |
| created_at | timestamp | NO | CURRENT_TIMESTAMP | 创建时间 |
| updated_at | timestamp | NO | CURRENT_TIMESTAMP | 更新时间 |

**索引**：
- PRIMARY KEY: id
- INDEX: last_message_time

---

#### 2.4.2 conversation_participants - 会话参与者表

**表说明**：记录会话成员及未读消息数

**字段列表**：

| 字段名 | 数据类型 | 是否为空 | 默认值 | 注释 |
|--------|---------|---------|--------|------|
| id | bigint UNSIGNED | NO | - | 参与者记录ID |
| conversation_id | varchar(50) | NO | - | 会话ID |
| user_id | int UNSIGNED | NO | - | 用户ID |
| unread_count | int UNSIGNED | NO | 0 | 未读消息数 |
| joined_at | timestamp | NO | CURRENT_TIMESTAMP | 加入时间 |

**索引**：
- PRIMARY KEY: id
- UNIQUE: (conversation_id, user_id)
- INDEX: user_id, conversation_id

**外键**：
- conversation_id → conversations(id) ON DELETE CASCADE
- user_id → users(user_id) ON DELETE CASCADE

---

#### 2.4.3 messages - 消息表

**表说明**：存储会话中的所有消息

**字段列表**：

| 字段名 | 数据类型 | 是否为空 | 默认值 | 注释 |
|--------|---------|---------|--------|------|
| id | varchar(50) | NO | - | 消息唯一ID |
| conversation_id | varchar(50) | NO | - | 会话ID |
| sender_id | int UNSIGNED | NO | - | 发送者用户ID |
| content | text | NO | - | 消息内容 |
| type | varchar(20) | NO | 'text' | 消息类型：text=文本，image=图片，file=文件 |
| created_at | timestamp | NO | CURRENT_TIMESTAMP | 发送时间 |

**索引**：
- PRIMARY KEY: id
- INDEX: conversation_id, sender_id, created_at

**外键**：
- conversation_id → conversations(id) ON DELETE CASCADE
- sender_id → users(user_id) ON DELETE CASCADE

---


### 2.5 互动功能模块

#### 2.5.1 favorites - 商品收藏表

**表说明**：用户收藏商品

**字段列表**：

| 字段名 | 数据类型 | 是否为空 | 默认值 | 注释 |
|--------|---------|---------|--------|------|
| favorite_id | bigint UNSIGNED | NO | - | 收藏记录唯一ID |
| goods_id | int UNSIGNED | NO | - | 商品ID |
| user_id | int UNSIGNED | NO | - | 收藏用户ID |
| created_at | timestamp | NO | CURRENT_TIMESTAMP | 收藏时间 |

**索引**：
- PRIMARY KEY: favorite_id
- UNIQUE: (user_id, goods_id)
- INDEX: user_id, goods_id, created_at

**外键**：
- goods_id → goods(good_id) ON DELETE CASCADE
- user_id → users(user_id) ON DELETE CASCADE

---

#### 2.5.2 notifications - 通知表

**表说明**：系统消息推送

**字段列表**：

| 字段名 | 数据类型 | 是否为空 | 默认值 | 注释 |
|--------|---------|---------|--------|------|
| id | bigint UNSIGNED | NO | - | 通知ID |
| user_id | int UNSIGNED | NO | - | 接收通知的用户ID |
| type | varchar(50) | NO | - | 通知类型：new_goods/exchange_request/exchange_accepted等 |
| title | varchar(200) | NO | - | 通知标题 |
| content | text | NO | - | 通知内容 |
| related_id | varchar(50) | YES | NULL | 关联ID（商品ID或交换ID） |
| related_type | varchar(50) | YES | NULL | 关联类型：goods, exchange |
| is_read | tinyint UNSIGNED | NO | 0 | 是否已读：0=未读，1=已读 |
| created_at | timestamp | NO | CURRENT_TIMESTAMP | 创建时间 |

**索引**：
- PRIMARY KEY: id
- INDEX: user_id, is_read, created_at, type

**外键**：
- user_id → users(user_id) ON DELETE CASCADE

---


### 2.6 违规管理模块

#### 2.6.1 violation_reports - 违规举报表

**表说明**：用户举报违规内容

**字段列表**：

| 字段名 | 数据类型 | 是否为空 | 默认值 | 注释 |
|--------|---------|---------|--------|------|
| report_id | int UNSIGNED | NO | - | 举报记录唯一ID |
| report_number | varchar(50) | NO | - | 举报编号，格式：VR-{时间戳}-{随机码} |
| target_type | varchar(20) | NO | - | 举报对象类型：product=商品举报，user=用户举报 |
| target_id | int UNSIGNED | NO | - | 被举报对象ID（商品ID或用户ID） |
| target_name | varchar(100) | YES | NULL | 被举报对象名称（冗余字段，便于查询） |
| reporter_id | int UNSIGNED | NO | - | 举报人用户ID |
| report_type | varchar(50) | NO | - | 举报类型：product-fake/product-prohibited/user-harassment等 |
| title | varchar(200) | NO | - | 举报标题 |
| description | text | NO | - | 举报描述（简要说明） |
| detail_content | text | YES | NULL | 举报详细内容（完整描述） |
| evidence_images | json | YES | NULL | 证据图片URL数组（OSS存储） |
| status | varchar(20) | NO | 'pending' | 处理状态：pending/processing/approved/rejected |
| handler_id | int UNSIGNED | YES | NULL | 处理人ID（管理员） |
| handle_action | varchar(20) | YES | NULL | 处理行动：warning/tempBan/permBan/removeProduct |
| handle_result | text | YES | NULL | 处理结果说明 |
| reject_reason | varchar(500) | YES | NULL | 驳回原因 |
| credit_deduction | tinyint UNSIGNED | YES | NULL | 扣除信誉值（1-100） |
| ban_duration | tinyint UNSIGNED | YES | NULL | 封禁时长（天数） |
| ban_until | timestamp | YES | NULL | 封禁截止时间 |
| created_at | timestamp | NO | CURRENT_TIMESTAMP | 举报创建时间 |
| updated_at | timestamp | NO | CURRENT_TIMESTAMP | 最后更新时间 |
| handled_at | timestamp | YES | NULL | 处理完成时间 |
| deleted_at | timestamp | YES | NULL | 软删除时间 |

**索引**：
- PRIMARY KEY: report_id
- UNIQUE: report_number
- INDEX: reporter_id, target_type, target_id, status, handler_id
- COMPOSITE INDEX: (target_type, target_id), (status, created_at)

**外键**：
- reporter_id → users(user_id) ON DELETE CASCADE
- handler_id → users(user_id)

---

#### 2.6.2 violation_report_logs - 举报处理日志表

**表说明**：记录举报处理全流程

**字段列表**：

| 字段名 | 数据类型 | 是否为空 | 默认值 | 注释 |
|--------|---------|---------|--------|------|
| log_id | bigint UNSIGNED | NO | - | 日志唯一ID |
| report_id | int UNSIGNED | NO | - | 关联的举报记录ID |
| operator_id | int UNSIGNED | NO | - | 操作人ID（管理员） |
| action_type | varchar(20) | NO | - | 操作类型：assign/process/approve/reject/comment |
| action_content | text | YES | NULL | 操作内容/备注 |
| old_status | varchar(20) | YES | NULL | 变更前状态 |
| new_status | varchar(20) | YES | NULL | 变更后状态 |
| created_at | timestamp | NO | CURRENT_TIMESTAMP | 操作时间 |

**索引**：
- PRIMARY KEY: log_id
- INDEX: report_id, operator_id, action_type
- COMPOSITE INDEX: (report_id, created_at)

**外键**：
- report_id → violation_reports(report_id) ON DELETE CASCADE
- operator_id → users(user_id)

---


## 三、数据库设计特点

### 3.1 外键约束
- 所有关联表使用外键约束（FOREIGN KEY）
- 级联删除（ON DELETE CASCADE）：删除主记录时自动删除关联记录
- 限制更新（ON UPDATE RESTRICT）：防止误操作

### 3.2 索引策略
- **主键索引**：所有表使用AUTO_INCREMENT主键
- **唯一索引**：username、email、phone、order_number等业务唯一字段
- **单列索引**：高频查询字段（user_id、status、created_at等）
- **复合索引**：多条件查询优化（idx_status_shelf、idx_target_type_id等）

### 3.3 字符集编码
- 统一使用utf8mb4字符集
- 排序规则：utf8mb4_unicode_ci
- 支持emoji和多语言字符

### 3.4 时间戳管理
- `created_at`：创建时间，默认CURRENT_TIMESTAMP
- `updated_at`：更新时间，自动更新ON UPDATE CURRENT_TIMESTAMP
- 业务时间：completed_at、cancelled_at、expires_at等

### 3.5 数据类型选择
- **ID字段**：INT UNSIGNED（用户/商品）、BIGINT UNSIGNED（日志/消息）、VARCHAR(50)（UUID）
- **状态字段**：VARCHAR(20)或TINYINT UNSIGNED枚举值
- **计数字段**：INT UNSIGNED DEFAULT 0
- **文本字段**：VARCHAR(长度)、TEXT（长内容）
- **JSON字段**：存储图片数组等复杂数据

### 3.6 安全设计
- **密码**：password_hash存储加密后的密码（bcrypt）
- **会话**：token/refresh_token双token机制
- **审计**：login_logs、violation_report_logs记录关键操作
- **权限**：user_type区分管理员和普通用户
- **软删除**：deleted_at字段实现逻辑删除

### 3.7 性能优化
- **分页查询**：created_at索引支持时间排序
- **计数缓存**：view_count、like_count、favorite_count避免实时统计
- **未读消息**：unread_count字段避免COUNT查询
- **最后消息**：last_message_id、last_message_time冗余提升查询效率
- **复合索引**：针对多条件查询场景优化

---


## 四、表关系图

```
users (核心表)
  ├─→ goods (发布商品)
  ├─→ orders (发起/接收交换)
  ├─→ favorites (收藏商品)
  ├─→ notifications (接收通知)
  ├─→ user_sessions (登录会话)
  ├─→ user_login_logs (登录日志)
  ├─→ conversation_participants (参与会话)
  ├─→ messages (发送消息)
  ├─→ violation_reports (举报/处理)
  └─→ violation_report_logs (操作日志)

goods
  ├─→ orders (交换商品)
  ├─→ favorites (被收藏)
  ├─→ conversations (关联会话)
  └─→ goods_categories (所属分类)

conversations
  ├─→ conversation_participants (参与者)
  └─→ messages (消息记录)

violation_reports
  └─→ violation_report_logs (处理日志)
```

---

## 五、核心业务流程

### 5.1 用户注册登录流程
1. 用户注册 → user_id_sequence生成ID → users表创建记录
2. 用户登录 → 验证密码 → user_sessions创建会话 → user_login_logs记录日志

### 5.2 商品发布流程
1. 用户发布商品 → goods表（status=0待审核）
2. 管理员审核 → 更新status（1已通过/2已拒绝）、auditor_id
3. 审核通过 → 用户上架 → shelf_status=1已上架

### 5.3 交换流程
1. 用户A浏览商品 → 发起交换请求 → orders表（status=pending）
2. 用户B收到通知 → 确认交换 → receiver_confirmed=1
3. 双方确认 → status=completed → goods.shelf_status=2已售出
4. 双方评价 → initiator_review、receiver_review

### 5.4 即时通讯流程
1. 用户发起聊天 → conversations表创建会话
2. 添加参与者 → conversation_participants表
3. 发送消息 → messages表 → 更新unread_count
4. 读取消息 → unread_count清零

### 5.5 违规举报流程
1. 用户举报 → violation_reports表（status=pending）
2. 管理员分配 → handler_id → violation_report_logs记录
3. 管理员处理 → status=processing → 执行handle_action
4. 完成处理 → status=approved/rejected → 扣除信用分/封禁用户

---


## 六、维护建议

1. **定期清理**：清理过期会话（user_sessions.expires_at）
2. **数据归档**：归档历史订单和消息数据
3. **性能监控**：监控索引使用情况，优化慢查询
4. **数据备份**：定期备份数据库
5. **审核效率**：审查违规举报处理效率
6. **信用监控**：监控用户信用评分分布

---

## 七、数据表汇总

| 序号 | 表名 | 中文名称 | 字段数 | 记录数量级 | 主要功能 |
|------|------|----------|--------|-----------|----------|
| 1 | users | 用户表 | 22 | 万级 | 用户账号、认证、信用管理 |
| 2 | user_id_sequence | 用户ID序列表 | 2 | 万级 | 生成顺序用户ID |
| 3 | user_sessions | 用户会话表 | 11 | 十万级 | 登录会话、token管理 |
| 4 | user_login_logs | 登录日志表 | 10 | 百万级 | 登录历史、安全审计 |
| 5 | goods | 商品表 | 21 | 十万级 | 商品信息、审核、统计 |
| 6 | goods_categories | 商品分类表 | 6 | 百级 | 商品分类管理 |
| 7 | orders | 订单表 | 24 | 十万级 | 交换订单、评价管理 |
| 8 | conversations | 会话表 | 9 | 十万级 | 聊天会话管理 |
| 9 | conversation_participants | 会话参与者表 | 5 | 十万级 | 会话成员、未读消息 |
| 10 | messages | 消息表 | 6 | 千万级 | 聊天消息记录 |
| 11 | favorites | 收藏表 | 4 | 百万级 | 商品收藏 |
| 12 | notifications | 通知表 | 9 | 百万级 | 系统通知推送 |
| 13 | violation_reports | 违规举报表 | 22 | 万级 | 违规举报管理 |
| 14 | violation_report_logs | 举报处理日志表 | 8 | 十万级 | 举报处理审计 |

**总计**：14张表，149个字段

---

## 八、技术栈

- **数据库**：MySQL 8.0.45
- **字符集**：utf8mb4
- **排序规则**：utf8mb4_unicode_ci
- **存储引擎**：InnoDB
- **外键约束**：启用
- **事务支持**：完整ACID支持

---

## 九、ER图说明

### 核心实体关系

1. **用户-商品关系**：一对多（一个用户可以发布多个商品）
2. **用户-订单关系**：多对多（一个用户可以作为发起方或接收方参与多个订单）
3. **商品-订单关系**：多对多（一个商品可以参与多个交换订单）
4. **用户-会话关系**：多对多（通过conversation_participants中间表）
5. **会话-消息关系**：一对多（一个会话包含多条消息）
6. **用户-收藏关系**：多对多（用户可以收藏多个商品，商品可以被多个用户收藏）
7. **用户-举报关系**：一对多（一个用户可以提交多个举报）

---

**文档版本**：v2.0  
**更新日期**：2026-03-18  
**数据库版本**：exchange_system  
**文档作者**：系统架构组
